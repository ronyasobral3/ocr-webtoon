# Captura de tela
mss

# Processamento de imagem
opencv-python
numpy

# OCR
rapidocr-onnxruntime
# Manga OCR japonês (engine mangá JP no dashboard): modelo ~400MB, baixa no 1º uso.
# Requer torch (instalado pelo ultralytics/transformers acima).
manga-ocr

# Detecção de balões (YOLOv8)
# torch é instalado pelo ultralytics. Para GPU NVIDIA, instale torch E torchvision
# do MESMO índice CUDA (senão torchvision vem CPU-only e o YOLO quebra na GPU):
#   pip install torch==2.12.0+cu126 torchvision==0.27.0+cu126 --index-url https://download.pytorch.org/whl/cu126
ultralytics

# Tradução
deep-translator
# NLLB-200 (engine NLLB no dashboard): tradução local fiel, offline, na GPU.
# O modelo (~2.4GB, facebook/nllb-200-distilled-600M) baixa no 1º uso.
transformers
sentencepiece
# Tradução contextual via Ollama (opcional — só necessário se usar engine OLLAMA no dashboard)
# pip install ollama   ← instale separadamente; requer Ollama rodando em localhost:11434

# Overlay / GUI
PyQt6
PyQt6-WebEngine

# Utilitários
Pillow
huggingface_hub

# Desenvolvimento / testes
pytest
